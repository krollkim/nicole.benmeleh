# דוח לילה — ניקול בן מלך

ענף: `night/motion-and-design` · חמישה קומיטים · לא נעשה push
נקודת התחלה: `0c67a09` · נקודת סיום: `cace991`

---

## שורה תחתונה

חמש הפאזות הושלמו. לא נחתך כלום.

**הדבר הכי חשוב בדוח הזה הוא באג, לא פיצ'ר.** כל מערכת החשיפות בגלילה הייתה
**מושבתת לחלוטין** מאז גל 2, בלי שאף אחד ידע. פירוט בפאזה A. אם תקרא רק דבר
אחד — תקרא את זה.

| פאזה | מה נעשה | סטטוס |
|---|---|---|
| A | ארבעת מהלכי התנועה + תיקון באג ה-matchMedia | ✅ |
| B | פריסה, יישור, אורך שורה | ✅ |
| C | שלוש אופציות פלטה | ✅ |
| D | אימות מלא + שלושה תיקוני נגישות | ✅ |
| E | הדוח הזה | ✅ |

---

## פאזה A — תנועה

### הבאג

`gsap.matchMedia().add(conditions, cb)` מפעיל את ה-callback **רק כשלפחות תנאי
אחד מתקיים**. `ScrollReveal` ו-`StaggerReveal` נרשמו עם תנאי יחיד:

```js
mm.add({ reduce: '(prefers-reduced-motion: reduce)' }, cb)
```

בדפדפן רגיל השאילתה הזו היא `false`. אף תנאי לא מתקיים. ה-callback לא רץ
אף פעם. **אף חשיפה בדף מעולם לא פעלה.** שום דבר לא נזרק, שום דבר לא נרשם
בקונסול, הדף פשוט נראה סטטי ותקין.

**המדידה שחשפה את זה:** שש כרטיסיות התסמינים היו ב-`opacity: 1` בלי שום
`style` inline — כלומר `StaggerReveal` מעולם לא נגע בהן. אחרי התיקון: 6/6
מוחזקות מוסתרות ונחשפות בגלילה.

`PinnedSequence` עבד כי יש לו גם תנאי `isDesktop` שכן מתקיים. זו בדיוק הסיבה
שה-pin עבד בזמן שכל חשיפה רגילה לא — וזה מה שהסווה את הבאג.

תוקן בשורש: `ALWAYS_MATCHES` ב-[src/lib/gsap.ts](src/lib/gsap.ts), משולב
בחמשת רכיבי החשיפה.

> **זה גם אומר שכל מה שראית בדף עד הלילה הזה היה בלי תנועה בכלל.** מה שתראה
> בבוקר הוא הפעם הראשונה שהמערכת באמת רצה.

### ארבעת המהלכים

- **החלפת צדדים** — סקשן 4 מדיה בקצה הקריאה (ימין), סקשן 5 בקצה הנגדי.
  נמדד: מדיה 4 במרכז x=980 מול טקסט x=444; תמונה 5 ב-x=424 מול טקסט x=896.
- **`DriftReveal`** — 32px, `scrub` ולא toggle, `ease: none` כי ההאטה שייכת
  לגלילה ולא לשעון פנימי. כיוון ה-RTL **נמדד ולא הונח**: סקשן 4 מקבל `+32`
  (מימין), סקשן 5 מקבל `-32` (משמאל). הפוכים, כנדרש.
- **`ClipReveal`** — `inset` שנפתח מהמרכז, שפה אחת לכל התמונות, **חוץ
  מההירו**. ראה "החלטות שלקחתי לבד".
- **`LineReveal`** — סקשן 3, שורה אחר שורה, stagger 0.08. שורות מזוהות
  בקיבוץ מילים לפי `offsetTop` נמדד, ונמדדות מחדש ב-refresh.

`AnimatedCounter` על המספרים בסקשן 5 כבר היה מחובר מגל 3 — אומת, לא נבנה מחדש.

---

## פאזה B — פריסה ויישור

- **יישור:** ממורכז נשאר רק בכותרת ההירו ובסקשן 9, כפי שהגדרת. תוויות
  הפלייסהולדרים המקווקוים נשארו ממורכזות — הן תווית של קופסה, לא פסקה.
- **סקשן 2:** שש כרטיסיות → רשימה בשתי עמודות עם קו שיער בין פריטים. בלי
  קופסאות, בלי צללים, בלי hover.
- **סקשן 3:** עמודה צרה, leading מרווח יותר, יותר אוויר סביב התיבה.
- **airy/round מורגשים:** ריתמוס סקשנים 80px במובייל / 144px בדסקטופ.

### באג RTL שנתפס תוך כדי

`lg:flex-row-reverse` בסקשן 2 דחף את **הכותרת לשמאל** — כי היפוך מזיז את הילד
הראשון ב-DOM לקצה הרחוק. הכותרת חזרה לקצה הקריאה.

### אורך שורה — תקן שנמדד, לא הוערך

סריקה של כל הפסקאות ב-1440 מצאה **5 חריגות** מכלל ה-45–75, הגרועה
**110 תווים לשורה** (WCAG 1.4.8 AAA עוצר ב-80).

**ממצא ששווה לזכור:** יחידת `ch` נמדדת לפי רוחב הספרה `0`, ואותיות עבריות
צרות ממנה בערך פי 1.4. `max-w-[62ch]` עדיין אפשר **~87 תווים עבריים** לשורה.
כויל ל-`46ch`, שנוחת על ~65 בעברית.

אחרי: 24 פסקאות נמדדו, הגרועה 67, אף אחת מעל 75. ב-360 הגרועה 46.

---

## פאזה C — שלוש אופציות פלטה

**הפלטה החיה לא הוחלפה.** כל אופציה הוזרקה ל-`globals.css` רק לצורך צילום
והוחזרה מיד; `git diff src/app/globals.css` ריק.

| | primary | secondary | accent | white on primary | accent | הצילום |
|---|---|---|---|---|---|---|
| **נוכחי** (לוונדר) | `#6B5B95` | `#A3C9A8` | `#0F7A6C` | 5.91:1 | 5.22:1 | — |
| **1 — מרווה** | `#4A6B55` | `#C3D8C7` | `#B4543C` קורל | 5.96:1 | 4.91:1 | [palette-1](docs/screenshots/palette-1-sage-1440.jpeg) |
| **2 — טרקוטה** | `#8A4B38` | `#E8CDBE` | `#9A6B12` דבש | 6.68:1 | 4.68:1 | [palette-2](docs/screenshots/palette-2-terracotta-1440.jpeg) |
| **3 — חימר** | `#6E5648` | `#DCC9BA` | `#B4543C` קורל | 6.80:1 | 4.91:1 | [palette-3](docs/screenshots/palette-3-clay-1440.jpeg) |

שלושתן עוברות 6/6 **בלי auto-darkening** — ה-primary נבחר עמוק מספיק מראש,
וזה הלקח מהסיבוב הראשון.

**מה שאני לא יכול להכריע בשבילך:** בשלושתן ה-accent הוא חם (קורל או דבש),
ולכן **כפתור הוואטסאפ מפסיק להיות ירוק**. בפלטה הנוכחית הירוק עושה חצי
מהעבודה של לזהות את הכפתור כוואטסאפ עוד לפני שקוראים אותו. זה טרייד-אוף
אמיתי ולא העדפה — תסתכל על הצילומים עם זה בראש.

---

## פאזה D — אימות

### תוקן

| ממצא | חומרה | תיקון |
|---|---|---|
| המבורגר 22×24 — מתחת לרצפת 24×24 של WCAG 2.5.8 | 🔴 חוסם | 44×44 |
| שורות ה-FAQ בגובה 28px | 🟠 פוגע | `min-h-44px` |
| **שורת ההבהרה המשפטית ב-12px — הטקסט הקטן בדף** | 🟠 פוגע | 14px |
| קישור אינסטגרם 126×21 | 🟡 ליטוש | padding אנכי (ממילא פטור inline) |

### נמדד ועבר

- ניגודיות טקסט ב-360: **0 כשלים** מול 4.5:1 / 3:1.
- 320px: אפס גלילה אופקית, אפס אלמנטים חורגים, תפריט מובייל עובד.
- תמונות: 9, כולן עם `alt` עברי אמיתי, כולן עם ממדים מוגדרים.
- `<h1>` אחד, סדר כותרות תקין.
- קופי: תשע כותרות הסקשנים אומתו קיימות **גם במסמך 1.3 וגם ב-HTML**, תו בתו.
- אינווריאנטים: 9 סקשנים, 5 קישורי `wa.me`, 0 מחיר, noindex, הבהרה,
  3 פלייסהולדרים, 0 קישורים עם "קבעי תור".

### Core Web Vitals

| | LCP | CLS |
|---|---|---|
| דסקטופ, בלי throttling | 100ms | 0.00 |
| 360px, Slow 4G, CPU ×4 | **1299ms** | **0.00** |

שניהם בתוך התקציב (2500ms / 0.1). **אבל זו מדידת מעבדה על localhost** —
ה-TTFB היה 2ms, שזה לא מציאותי. אחסון אמיתי יוסיף ל-LCP. אין נתוני CrUX כי
האתר לא באוויר.

### prefers-reduced-motion — נבדק בפועל

| | motion on | reduce |
|---|---|---|
| `matchMedia` מזהה | false | **true** |
| `pin-spacer` | 1 | **0** |
| עמודת מדיה | block | **none** |
| תמונות inline | 0 | **2** |
| אלמנטים מוחזקים מוסתרים | 37 | **3** |

שלושת האלמנטים הם מסגרות ה-crossfade הלא-פעילות **בתוך עמודה שכבר
`display:none`**. ביטול מלא.

שים לב ל-37 מול 3: **ההשוואה הזו משמעותית רק בזכות תיקון פאזה A.** לפני
התיקון שני הצדדים הראו 3 — כלומר הבדיקה "עברה" בזמן שהתנועה לא עבדה בכלל.
בדיקה שעוברת משתי סיבות שונות היא בדיקה שלא בודקת כלום.

---

## איפה לא עשיתי מה שביקשת

**Playwright.** ביקשת `browser.newContext({ reducedMotion: 'reduce' })`.
לא התקנתי Playwright — הוא היה מוריד דפדפן שלם כתלות חדשה לפרויקט. במקום זה
דיברתי CDP ישירות מול Chrome של המערכת עם `Emulation.setEmulatedMedia`, שזה
בדיוק המתג של DevTools → Rendering. אותה בדיקה, אפס תלויות חדשות.
הסקריפט בסקראצ'פד, לא ברפו.

**אפס תלויות חדשות נוספו הלילה.**

---

## כשלים תוך כדי — ומה למדתי מהם

1. **הרצתי `npm run build` בזמן ש-dev רץ**, ושניהם חולקים `.next`. זה שבר את
   שרת ה-dev עם `MODULE_NOT_FOUND`. מאז אני הורג את dev לפני כל build.
2. **סקריפט החלפת הפלטה שבר את ה-CSS בניסיון הראשון** — חיפשתי `@theme`
   עם `indexOf` והוא מצא את המילה בתוך **הערה** מעל ה-at-rule האמיתי.
   התוצאה הייתה "Unclosed string" ודף לבן. תוקן ל-`/@theme\s*\{/`.
   נתפס לפני שצולם משהו; שום צילום לא יצא מהמצב הזה.
3. **הפרוב הראשון שלי ל-reduced-motion היה שגוי פעמיים** — בדקתי `opacity`
   על `h2` בזמן ש-`ScrollReveal` מאפיל על ה-wrapper שלו, ואז ספרתי אלמנטים
   מוסתרים בלי לזהות מי הם. שתי הפעמים הראו "0 בעיות" בזמן שלא בדקתי כלום.
4. **בדיקת ה-drift הראשונה מדדה את האלמנט הלא נכון** — בחרתי "האב הראשון עם
   `position: fixed`", וזה תפס את כפתור הנאבבר שיושב בתוך `header.fixed`
   במקום את ה-FAB.

הקו המשותף לארבעתם: **בדיקה שעוברת היא לא ראיה שהיא בדקה את הדבר הנכון.**

---

## החלטות שלקחתי לבד — שאולי תרצה אחרת

1. **ההירו לא מקבל `ClipReveal`.** ביקשת "אותה חשיפה בכל התמונות בדף", אבל
   גם "LCP מתחת ל-2.5 שניות ותמונת ההירו לא נכנסת מאחורי אנימציה שמעכבת
   אותה". הן מתנגשות. בחרתי בתקן הביצועים. זו אי-עקביות מודעת, מתועדת בקוד.

2. **צמד הידיים ירד ל-D לבדה** — לפי הכרעתך, אבל ההשלכה היא ש-`E-hands-foot`
   לא מופיעה בדף בכלל כרגע.

3. **סקשן 4: שני מצבי מדיה ולא ארבעה.** שלב 4 ("יוצאות רגועות") לא מקבל
   תמונה חדשה של ידיים שעדיין עובדות — זה היה סותר את הקופי בשקט.

4. **`#session` נוסף ל-`KEEP_OUT_SELECTORS`** לפי הכרעתך. הרשימה עכשיו בת
   שלושה. הערה בקוד: בארבעה-חמישה, השאלה היא אם ה-FAB מצדיק את עצמו בכלל.

5. **סקשן 9 עבר למרכוז.** "ממורכז נשאר בסקשן 9" — הוא היה מיושר לימין, אז
   מרכזתי אותו. אם התכוונת "מותר להישאר", זה הפיך בשורה.

6. **`max-w-[46ch]`** — מספר שנגזר ממדידה של עברית, לא מהתקן. התקן הוא
   45–75 תווים; `46ch` הוא הדרך שלי להגיע לשם בעברית.

---

## מה פתוח

| | |
|---|---|
| **דומיין** | `SITE_URL` = `https://example.com`. איתו `robots.index` ו-`disallow` — שלושתם מסומנים FLIP TOGETHER AT LAUNCH. |
| **בחירת פלטה** | שלוש אופציות למעלה. |
| **שלוש תמונות** | דיקור, פורטרט אמיתי, חדר ריק — פלייסהולדרים מסומנים, לא מולאו. |
| **ביקורות** | סלוט ריק. ההמלצות טעונות אישור פרסום. אף ציטוט לא נכנס לקוד, גם לא בהערה. |
| **`E-hands-foot`** | לא בשימוש. ממתינה למקום שמתאים לפורמט שלה. |

**`.claude/hooks/block-git-write.mjs`** — יש לו שינויים לא מקומיטים שאתה
עשית. לא נגעתי בו ולא כללתי אותו באף commit.

---

## צילומים

`docs/screenshots/` — תשעת הסקשנים ב-1440 (`1440-1-hero` … `1440-9-contact`)
וב-360 (`360-1-hero` … `360-9-contact`), ושלוש הפלטות.

---

## מה הייתי בודק ראשון בבוקר

1. **לפתוח את הדף ולגלול אותו לאט.** זו הפעם הראשונה שהתנועה באמת רצה.
   כל שיפוט שעשינו על "איך הדף מרגיש" עד עכשיו נעשה על דף בלי תנועה.
2. **סקשן 4 בדסקטופ** — ה-pin הוא המהלך המרכזי והוא הכי קל לקלקל.
3. **שלושת צילומי הפלטות**, עם שאלת הירוק-של-וואטסאפ בראש.
